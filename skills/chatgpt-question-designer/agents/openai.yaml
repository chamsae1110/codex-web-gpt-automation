interface:
  display_name: "ChatGPT Question Designer"
  short_description: "Design regular missions and explicit full-access agentic Pro routes."
  default_prompt: "Design a purpose-specific GPT mission using Oracle+DevSpace; default to the highest-tier non-Pro route and select read-only qualified Pro only after explicit user opt-in. Route writes and commands to regular GPT-5.6 extra-high. Use `pro-attachment` only for DevSpace-unreadable immutable evidence. Never route new work through CodexPro."
